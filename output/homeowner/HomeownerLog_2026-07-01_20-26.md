# Homeowner Run — arena · 2026-07-01

## Brief
Build the Community Arena for BananaOrNoBanana (working directory H:\BananaOrNoBanana), autonomously. The behavior spec already exists and is build-ready at Planning/specs/arena_spec.md — adopt it as the source of truth; do NOT re-derive a thinner spec. Context: SPEC.md (roadmap + standard-conditions), Planning/specs/banana_spec.md (the existing game), server/ (Python app), docs/decisions/banana_2026-07-01_*.md.

The arena spec defines four features to build: (1) Client submission — opt-in `--submit` flag on the batch runner plus a `server/submit` module, uuid client_id, local submitted-tracking, wire format v1; this is Python, added to the existing server/ package and tests to the pytest suite. (2) Worker ingest (POST /api/submit) — a SEPARATE program in JS/TS under arena/ (wrangler.toml, src/, schema.sql, test/), with validation, server-side rescore, dedupe, rate limits, community/verified tiers, D1 storage. (3) Public leaderboard (GET /api/leaderboard + static page) — same aggregation rules as server/stats.py, with a shared-fixture test asserting the Worker and Python agree. (4) Arena deployment & repo layout.

Key constraints: the wire format IS the seat-aware rounds.jsonl schema (including temperature + standard_settings); the Worker recomputes winner/correct and standard_settings server-side rather than trusting the client; no secrets committed. The [human-required] Done-when items (live Cloudflare deploy, real submission, browser leaderboard render) can't be verified autonomously — surface them as manual follow-ups rather than blocking. Halt and surface if the spec has Open Questions that would change the build (note: there is one about model-alias grouping, already resolved with a v1 default of strict strings — proceed on that default). Follow CLAUDE.md change rules; every change ends in a commit; no Claude attribution in commits or artifacts.

## Phase 1 — Scaffold (greenfield only)
Greenfield: no → skipped (existing project — Planning/ and CLAUDE.md both present).

## Phase 2 — Spec
Architect subagent (plumbline:architect, autonomous mode) spawned to validate/adopt the existing Planning/specs/arena_spec.md.
**BLOCKED — orchestration failure, not a spec gap.** The architect subagent returned a no-op twice (0 tool uses; reads the architect SKILL.md then stops). This reproduces the documented Plumbline **v1.0.0** subagent bug (lowercase `tools:` frontmatter dropping agent tool grants; fixed in v1.0.1). The plugin is loaded from the 1.0.0 cache, so all four pipeline subagents (architect/foreman/builder/inspector) are expected to fail the same way.
Decision (human): fix the tooling and re-run homeowner properly (keep the independent-inspector guarantee).

**Fix applied:** confirmed the bug — 1.0.0 agents carry `tools: [read, write]` (lowercase = invalid → tool grants dropped → agents spawn tool-less, emitting `<invoke>` as literal text). Source repo is already v1.0.1 with `tools: [Read, Write, Edit, Glob, Grep]`. Ran `claude plugin marketplace update plumbline` then `claude plugin update plumbline@plumbline` → installed 1.0.0 → **1.0.1** (verified via `claude plugin list`; cache now holds both 1.0.0 and 1.0.1).

**Still halted — session restart required.** The plugin update reported "Restart to apply changes." A re-test spawn of plumbline:architect after the update STILL no-op'd (0 tool uses, literal `<invoke>` text) — this session loaded the 1.0.0 agent definitions at startup and cannot hot-swap them. The fix is on disk but not live in this process.

**Resume path:** start a fresh `claude` session (loads 1.0.1 agents) and re-run `/homeowner` with the same brief. The spec is build-ready; the pipeline should run clean with working subagents. One conformance fix is still owed and will be handled by the Phase-2 architect on the fresh run: downgrade the model-alias "Open Question" (already resolved with a v1 default of strict strings) to an Assumption so it doesn't read as blocking.

Nothing was committed this run; the working tree is unchanged except this log (and the plugin install, which is outside the repo).
