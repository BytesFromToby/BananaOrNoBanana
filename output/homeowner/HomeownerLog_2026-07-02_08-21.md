# Homeowner Run — role_rotation · 2026-07-02

## Brief
> We have new specs for the Banana game. run /homeowner on it

Interpreted: build the newly-specced feature **"Red/Blue players & role rotation"** (added to
`Planning/specs/banana_spec.md` on 2026-07-02, commit c95c942 + uncommitted finalization edits) to
verified code. This is an existing FastAPI/vanilla-JS project; every other feature in the spec is
already built and tested. The new feature is spec-only so far (no `RED_PLAYER_*`, `/hold`,
`HUMAN_ROLE`, or `holder_color` in `server/`). Two-human party mode is finalized as a permanent
non-goal.

## Phase 1 — Scaffold (greenfield only)
Greenfield: no → skipped (existing project: Planning/ and CLAUDE.md present).

## Phase 2 — Spec
Architect status: READY · ASSUMPTIONS: 5 + SIZE_FLAGGED
Spec: Planning/specs/banana_spec.md (feature block `## Feature: Red/Blue players & role rotation`)
Decision log: docs/decisions/role_rotation_2026-07-02.md (created)
Architect made no spec edits — the human-authored feature block was already build-ready (8 Done-when
items: 7 [automated], 1 [human-required]; no Open Questions).
ASSUMPTIONS (5, verbatim):
- Hard rename `LEFT_PLAYER_*`→`RED_PLAYER_*`, `RIGHT_PLAYER_*`→`BLUE_PLAYER_*`; the local `.env` is migrated in place (single local user; no long back-compat window).
- Rotation starts with Red as Box Holder in game 1, then alternates.
- Rotation state is in-memory and per-process (resets on restart); acceptable since rounds are already ephemeral and one-at-a-time.
- Human role choice is a single setting (`HUMAN_ROLE`, default `guesser`). At most one player is human; two-human games are a permanent non-goal.
- The per-round `model` override is retired rather than reinterpreted.

## Phase 3 — Spec self-review
Verdict: FAITHFUL
- Faithfulness: every brief item present in the feature block (Red/Blue rename, per-game role rotation
  [AI-vs-AI alternates / human chooses via HUMAN_ROLE], human Box Holder `/hold` path, unchanged
  scoring, holder_color/guesser_color in round state + log, GET/PUT /api/players by color, deprecated
  per-round model override, two-human = permanent non-goal). Nothing dropped; no hallucinated scope;
  every default recorded in `## Assumptions (Red/Blue rotation)`.
- Assumptions: 5 reviewed with the §6 cost test; each is a cheap, reversible, low-surprise default
  consistent with the existing ephemeral one-round-at-a-time architecture. None hides a build-forking
  decision → none needs re-filing as an Open Question.
- Tractability: SIZE_FLAGGED concerns the monolithic spec FILE (~10 feature blocks > 6), of which 9 are
  already built. This run builds ONE new feature block — well within a single autonomous pass. The
  size flag is carried to the closing report as a spec-file restructure recommendation; it does not
  make this scoped feature build TOO_BIG. Foreman/builder are scoped to the new feature only.

## Phase 4 — Blueprint
Foreman status: BLUEPRINT_READY
Blueprint: Planning/blueprints/role_rotation_BP.md (single file)
- Slices: 5. [inspect]-flagged: 1 (rename/.env migration/log schema), 2 (rotation engine seam),
  3 (human holder /hold + no-leak security + create-response schema), Final (always). Slice 4
  (frontend) unflagged.
- Every [automated] Done-when item has a planned committed test (DW1,2,3,4,5,6,8); DW7 is the sole
  [human-required] item (UI, evidence via playwright in Final Slice).
- Forward constraint written into Slice 1 Step 4: log-field additions must not remove/rename existing
  fields, preserving arena↔server/stats.py shared-fixture parity; Final Slice runs `cd arena; npm test`.

## Phase 5 — Build til green
Inspection level: flagged
- Slice 1 [inspect]: PASS (Inspector 2026-07-02 09:17). Suite 115 passed; DW6 fidelity ok; arena
  parity intact (npm 35 passed); no-leak not regressed. Non-blocking note: HUMAN_ROLE config lacks a
  dedicated unit test (exercised by DW3 in Slice 3) — passed to builder to close.
- Slice 2 [inspect]: IN PROGRESS — halted. Step 1 complete (`assign_roles` + in-memory rotation
  state, `tests/test_rotation.py` unit tests present, [x]). Steps 2–6 NOT done (create_round role
  assignment via assign_roles + rotation advance, expose assigned colors to client, DW1/DW8/DW2
  tests). No PASS stamp — inspection not yet due.
- Builder HUMAN_ROLE gap-closer: added to `tests/test_config.py` (suite now 121 passed).

Builder final status: HALTED (environmental) — subagent hit an account session/usage limit
(resets 10:20am America/Chicago). NOT a `STUCK` and NOT a spec contradiction; the build simply
could not continue. Suite green at 121 passed at the halt.
Deviations: output/deviations/Deviations_role_rotation_2026-07-02_09-14.md (2 logged, both name/
sequencing migrations, no behavior change) + 1 inline Slice-2 Step-1 deviation in the blueprint.

## Phase 6 — Independent sign-off
Not reached — run halted in Phase 5.

Outcome: HALTED at Phase 5 (build til green) — environmental usage limit mid Slice 2. Working tree
left intact and uncommitted for resume.
